# DashZW local/production stack placeholders
# TODO(postgresql): replace json-file store with postgres service
# TODO(redis): enable caching / sessions / rate limits

services:
  api:
    build: .
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      API_VERSION: v1
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-me}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176}
      # DATABASE_URL: postgresql://dashzw:dashzw@postgres:5432/dashzw
      # REDIS_URL: redis://redis:6379
    volumes:
      - api-data:/app/backend/data
    # depends_on:
    #   - postgres
    #   - redis

  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: dashzw
  #     POSTGRES_PASSWORD: dashzw
  #     POSTGRES_DB: dashzw
  #   volumes:
  #     - pg-data:/var/lib/postgresql/data

  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

volumes:
  api-data:
  # pg-data:
